apiVersion: 1
providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # This path matches the volume mount in your docker-compose.yml
      path: /var/lib/grafana/dashboards

